<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.matczakonline.zaliczeniowa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pl.matczakonline.zaliczeniowa.common.activities.HomeActivity"
            android:noHistory="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".common.activities.SignUPActivity" />
        <activity android:name=".common.activities.TodoAppActivity" android:label="ToDo App" />
        <activity android:name=".common.activities.AddTaskActivity" />
    </application>

</manifest>
